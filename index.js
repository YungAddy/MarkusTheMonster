import Typed from "https://cdn.jsdelivr.net/npm/typed.js@2.0.12"
var options = {
  strings: ['<i>First</i> sentence.', '&amp; a second sentence.'],
  typeSpeed: 40
};

var typed = new Typed('#markusTyped', options);
